/*
  Interval settings:
  octave 1: root, third, fifth
  octave 2: root, mintor third, fourth, seventh
  octave 3: root
*/

const intervalSettings = [1, 5 / 4, 3 / 2, 2, (6 / 5) * 2, (4 / 3) * 2, (7 / 4) * 2, 3];

export default intervalSettings;
